// FoodFriend/src/config.ts
export const CONFIG = {
  // IMPORTANT: Update this to your Mac's current IP address (e.g., 192.168.1.XX)
  // Run 'ipconfig getifaddr en0' in your terminal to find it.
  API_URL: 'http://10.0.0.117:3001', 
  TIMEOUT_MS: 10000,
};
